name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Log in to repo
      run: echo ${{ secrets.GITHUB_TOKEN }} | docker login -u=ikewai --password-stdin ghcr.io
       
