## Build Tools for Rainfall Containers

Contents:
* `mkdir_from_txt.py`: A (tiny) script that makes directories from a newline-delimited text file. It is expected that every directory is an absolute one.